// examples/demo.ltl — runnable demo for ltl_aes_gcm

import std::io
use ltl_aes_gcm as lib

fn main() {
    io::println("ltl_aes_gcm demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}